Authors: Tal Cohen + Mike Lynch

Calculates Sierpinski's triangle up to a depth of 5.

There is a macro called DEPTH that can be changed to determine how many depths to recruse.
Will not work if Depth exceeds 5, since overflow.